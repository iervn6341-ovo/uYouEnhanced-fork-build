diff --git a/headers/Util.h b/headers/Util.h
index 9d24990..9fd728d 100644
--- a/headers/Util.h
+++ b/headers/Util.h
@@ -7,6 +7,7 @@
 NS_ASSUME_NONNULL_BEGIN
 
 extern NSString * const FeedFilterStateDidChangeNotification;
+FOUNDATION_EXPORT NSArray<UIWindow *> *GonerinoActiveWindows(void);
 
 @interface FeedMetadataRecord : NSObject
 
diff --git a/sources/CustomSettings.m b/sources/CustomSettings.m
index 66d375e..02ec3b7 100644
--- a/sources/CustomSettings.m
+++ b/sources/CustomSettings.m
@@ -20,6 +20,16 @@ static void ShowToast(UIViewController *viewController, NSString *message) {
     [Util showToast:message fromView:viewController.view];
 }
 
+#pragma clang diagnostic push
+#pragma clang diagnostic ignored "-Wdeprecated-declarations"
+static void ConfigureLegacyButtonInsets(UIButton *button,
+                                        CGFloat imagePadding,
+                                        UIEdgeInsets contentInsets) {
+    button.imageEdgeInsets = UIEdgeInsetsMake(0.0, 0.0, 0.0, imagePadding);
+    button.contentEdgeInsets = contentInsets;
+}
+#pragma clang diagnostic pop
+
 static UIButton *NavigationBackButton(NSString *title, id target, SEL action) {
     UIButton *button = [UIButton buttonWithType:UIButtonTypeSystem];
     if (@available(iOS 15.0, *)) {
@@ -32,8 +42,8 @@ static void ShowToast(UIViewController *viewController, NSString *message) {
     } else {
         [button setImage:[UIImage systemImageNamed:@"chevron.backward"] forState:UIControlStateNormal];
         [button setTitle:title forState:UIControlStateNormal];
-        button.imageEdgeInsets = UIEdgeInsetsMake(0.0, 0.0, 0.0, 4.0);
-        button.contentEdgeInsets = UIEdgeInsetsMake(0.0, 12.0, 0.0, 12.0);
+        ConfigureLegacyButtonInsets(button, 4.0,
+            UIEdgeInsetsMake(0.0, 12.0, 0.0, 12.0));
     }
     button.contentHorizontalAlignment = UIControlContentHorizontalAlignmentLeft;
     button.frame = CGRectMake(0.0, 0.0, MAX(44.0, button.intrinsicContentSize.width), 44.0);
@@ -547,8 +557,9 @@ - (UITableViewCell *)buttonCellForTableView:(UITableView *)tableView indexPath:(
         NSString *symbol = prominent ? @"heart.fill" : (changelog ? @"sparkles" : (indexPath.row == 0 ? @"square.and.arrow.up" : @"square.and.arrow.down"));
         [button setImage:[UIImage systemImageNamed:symbol] forState:UIControlStateNormal];
         [button setTitle:title forState:UIControlStateNormal];
-        button.imageEdgeInsets = UIEdgeInsetsMake(0.0, 0.0, 0.0, 6.0);
-        button.contentEdgeInsets = UIEdgeInsetsMake(prominent ? 12.0 : 8.0, 0.0, prominent ? 12.0 : 8.0, 0.0);
+        CGFloat verticalInset = prominent ? 12.0 : 8.0;
+        ConfigureLegacyButtonInsets(button, 6.0,
+            UIEdgeInsetsMake(verticalInset, 0.0, verticalInset, 0.0));
     }
     button.tag = indexPath.section * 100 + indexPath.row;
     button.accessibilityLabel = title;
diff --git a/sources/Settings.x b/sources/Settings.x
index fb15c8c..c5583ce 100644
--- a/sources/Settings.x
+++ b/sources/Settings.x
@@ -1,6 +1,7 @@
 #import "Settings.h"
 #import "CustomSettings.h"
 #import "Localization.h"
+#import "Util.h"
 #import <objc/runtime.h>
 
 static void *SettingsManagerAssociationKey = &SettingsManagerAssociationKey;
@@ -54,7 +55,7 @@ static void AssociateSettingsManager(YTSettingsViewController *settingsViewContr
                              OBJC_ASSOCIATION_RETAIN_NONATOMIC);
     UINavigationController *navigationController = settingsViewController.navigationController;
     if (!navigationController) {
-        for (UIWindow *window in [UIApplication sharedApplication].windows) {
+        for (UIWindow *window in GonerinoActiveWindows()) {
             navigationController = NavigationControllerContaining(window.rootViewController,
                                                                    settingsViewController,
                                                                    0);
@@ -151,7 +152,7 @@ static YTSettingsViewController *SettingsViewControllerForManager(YTSettingsSect
     }
 
     Class settingsClass = %c(YTSettingsViewController);
-    for (UIWindow *window in [UIApplication sharedApplication].windows) {
+    for (UIWindow *window in GonerinoActiveWindows()) {
         UIViewController *candidate = TopVisibleViewController(window.rootViewController);
         if ([candidate isKindOfClass:settingsClass]) {
             return (YTSettingsViewController *)candidate;
@@ -166,7 +167,7 @@ static UINavigationController *SettingsNavigationController(YTSettingsViewContro
     UINavigationController *navigationController = settingsViewController.navigationController;
     if (navigationController)
         return navigationController;
-    for (UIWindow *window in [UIApplication sharedApplication].windows) {
+    for (UIWindow *window in GonerinoActiveWindows()) {
         navigationController = NavigationControllerContaining(window.rootViewController,
                                                                settingsViewController,
                                                                0);
diff --git a/sources/Tweak.x b/sources/Tweak.x
index 310dbbf..b46097b 100644
--- a/sources/Tweak.x
+++ b/sources/Tweak.x
@@ -1174,7 +1174,6 @@ static void PrepareNavigationButton(YTRightNavigationButtons *owner) {
         owner.actionButton.frame = CGRectMake(0, 0, 40, 40);
         owner.actionButton.autoresizingMask = UIViewAutoresizingFlexibleRightMargin | UIViewAutoresizingFlexibleBottomMargin;
         owner.actionButton.imageView.contentMode = UIViewContentModeCenter;
-        owner.actionButton.adjustsImageWhenHighlighted = NO;
         [owner.actionButton addTarget:owner action:@selector(actionButtonPressed:)
                       forControlEvents:UIControlEventTouchUpInside];
         [owner addSubview:owner.actionButton];
@@ -1229,8 +1228,10 @@ static void UpdateNavigationButton(YTRightNavigationButtons *owner) {
     owner.actionButton.hidden = !shouldShow;
     if (changed) {
         UIImage *image = isEnabled ? images.firstObject : images.lastObject;
-        if (image)
+        if (image) {
             [owner.actionButton setImage:image forState:UIControlStateNormal];
+            [owner.actionButton setImage:image forState:UIControlStateHighlighted];
+        }
         owner.actionButton.tintColor = pageStyle == UIUserInterfaceStyleDark ? UIColor.whiteColor : UIColor.blackColor;
         owner.actionButton.accessibilityLabel = LocalizedString(@"Gonerino");
         owner.actionButton.accessibilityValue = LocalizedString(isEnabled ? @"Enabled" : @"Disabled");
diff --git a/sources/Util.m b/sources/Util.m
index bed0faf..888b851 100644
--- a/sources/Util.m
+++ b/sources/Util.m
@@ -6,6 +6,22 @@
 
 NSString * const FeedFilterStateDidChangeNotification = @"FeedFilterStateDidChangeNotification";
 
+NSArray<UIWindow *> *GonerinoActiveWindows(void) {
+    NSMutableArray<UIWindow *> *foregroundWindows = [NSMutableArray array];
+    NSMutableArray<UIWindow *> *fallbackWindows = [NSMutableArray array];
+    for (UIScene *scene in UIApplication.sharedApplication.connectedScenes) {
+        if (![scene isKindOfClass:UIWindowScene.class])
+            continue;
+        NSArray<UIWindow *> *windows = ((UIWindowScene *)scene).windows;
+        [fallbackWindows addObjectsFromArray:windows];
+        if (scene.activationState == UISceneActivationStateForegroundActive ||
+            scene.activationState == UISceneActivationStateForegroundInactive) {
+            [foregroundWindows addObjectsFromArray:windows];
+        }
+    }
+    return foregroundWindows.count > 0 ? foregroundWindows.copy : fallbackWindows.copy;
+}
+
 static BOOL ObjectIsClassNamed(id object, NSString *className);
 static id DirectNamedValue(id object, NSString *key);
 static void AdaptShortsNode(id node, NSMutableArray<id> *objects, NSMutableSet *visited);
@@ -1874,7 +1890,7 @@ + (void)showToast:(NSString *)message fromView:(UIView *)view {
     dispatch_async(dispatch_get_main_queue(), ^{
         UIWindow *window = view.window;
         if (!window) {
-            for (UIWindow *candidate in [UIApplication sharedApplication].windows) {
+            for (UIWindow *candidate in GonerinoActiveWindows()) {
                 if (candidate.isKeyWindow) {
                     window = candidate;
                     break;
@@ -1882,7 +1898,7 @@ + (void)showToast:(NSString *)message fromView:(UIView *)view {
             }
         }
         if (!window)
-            window = [UIApplication sharedApplication].windows.firstObject;
+            window = GonerinoActiveWindows().firstObject;
         if (!window)
             return;
 

